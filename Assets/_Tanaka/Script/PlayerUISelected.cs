using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerUISelected : MonoBehaviour
{   
    // 処理内容どうするか

    // 1Buttonで変わるのでCanvasは必要そう

    // objがUIのImageを作成するScriptを書く?
    // 例 : UIの親をManagerから取得(parentObj)
    // parentObjの子供としてImageをInstance、座標はobjがそれぞれ持つ
    // その座標を参照し移動

    // 2別口作成(それっぽい場所にUIを配置し作成)

    // 1だとなんか回りくどい、2だと汎用性は無い(5つの時と4つの時で配置換えが必要

    // 他に何かあるか、

    // GridRayOutGroupなる物があるらしい

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
